{
  "name": "@delegare/x402",
  "version": "0.5.20",
  "description": "AP2-native x402 + MPP middleware for Express. Dual-rail by default (USDC on Base + Stripe fiat). Auto-discoverable on CDP Bazaar, MPPScan, and Delegare Market in one call. Agents pay against bounded SD-JWT-VC mandates — no keys, no card details. Settlement fee: $0.005 min (includes Base chain fee), 3% under $1, capped at $0.03.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/delegare/delegare.git"
  },
  "keywords": [
    "x402",
    "mpp",
    "mppscan",
    "ap2",
    "agentic-payments",
    "bazaar",
    "cdp",
    "delegare-market",
    "dual-rail",
    "micropayments",
    "stripe",
    "ai",
    "agents",
    "express",
    "middleware",
    "usdc",
    "base"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --passWithNoTests",
    "lint": "tsc --noEmit",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {},
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
